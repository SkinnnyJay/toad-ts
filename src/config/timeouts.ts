export const TIMEOUT = {
  BATCH_DELAY_MS: 300,
  DISCONNECT_MS: 500,
  DISCONNECT_FORCE_MS: 500,
  SQLITE_BUSY_MS: 5000,
  BACKOFF_BASE_MS: 250,
  BACKOFF_CAP_MS: 5000,
  AUTO_APPROVE_DISABLED: 0,
  AUTO_APPROVE_DEFAULT: 5,
  THROTTLE_MS: 100,
  RESIZE_DEBOUNCE_MS: 50,
  MS_PER_SECOND: 1000,
  SESSION_BOOTSTRAP_MS: 8000,
  STREAM_BUFFER_MS: 16,
  MENTION_DEBOUNCE_MS: 150,
  WEBFETCH_MS: 10000,
  HOOK_PROMPT_MS: 15000,
  COMPACTION_SUMMARY_MS: 15000,
  LEADER_KEY_TIMEOUT_MS: 1200,
  DOUBLE_ESCAPE_MS: 600,
  TERMINAL_KILL_GRACE_MS: 500,
  TOOL_CALL_SUMMARY_PULSE_MS: 500,
  COMMAND_DISCOVERY_MS: 3000,
  BEADS_COMMAND_MS: 10000,
} as const;

// Re-export for convenience
export const {
  BATCH_DELAY_MS,
  DISCONNECT_MS,
  DISCONNECT_FORCE_MS,
  SQLITE_BUSY_MS,
  BACKOFF_BASE_MS,
  BACKOFF_CAP_MS,
  AUTO_APPROVE_DISABLED,
  AUTO_APPROVE_DEFAULT,
  THROTTLE_MS,
  RESIZE_DEBOUNCE_MS,
  MS_PER_SECOND,
  SESSION_BOOTSTRAP_MS,
  STREAM_BUFFER_MS,
  MENTION_DEBOUNCE_MS,
  WEBFETCH_MS,
  HOOK_PROMPT_MS,
  COMPACTION_SUMMARY_MS,
  LEADER_KEY_TIMEOUT_MS,
  DOUBLE_ESCAPE_MS,
  TERMINAL_KILL_GRACE_MS,
  TOOL_CALL_SUMMARY_PULSE_MS,
  COMMAND_DISCOVERY_MS,
  BEADS_COMMAND_MS,
} = TIMEOUT;
