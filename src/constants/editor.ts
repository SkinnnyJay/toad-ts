export const EDITOR = {
  DEFAULT_COMMAND: "vi",
  TEMP_DIR_PREFIX: "toadstool-editor-",
  TEMP_FILE_NAME: "message.md",
} as const;

export const { DEFAULT_COMMAND, TEMP_DIR_PREFIX, TEMP_FILE_NAME } = EDITOR;
