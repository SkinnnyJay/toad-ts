/**
 * Display name and terminal title for the app (used for process title and xterm window title).
 */
export const APP_BRANDING = {
  TERMINAL_TITLE: "🍄 toadstool",
} as const;

export const { TERMINAL_TITLE } = APP_BRANDING;
